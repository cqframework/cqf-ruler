hapi:
   fhir:
      cdshooks:
         enabled: true
         fhirserver:
            expandValueSets: true
            maxCodesPerQuery: 64
            searchStyle: GET
         prefetch:
            maxUriLength: 8000
         useRemoteData: true

