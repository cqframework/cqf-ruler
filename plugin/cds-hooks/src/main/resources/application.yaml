hapi:
   fhir:
      cdshooks:
         enabled: true
         fhirserver:
            maxCodesPerQuery: ~
            expandedValueSets: ~
            searchStyle: ~
         prefetch:
            maxUriLength: ~
