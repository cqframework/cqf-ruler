hapi:
   fhir:
      cdshooks:
         enabled: true
         clientIdHeaderName: client_id
         fhirserver:
            expandValueSets: true
            maxCodesPerQuery: 64
            queryBatchThreshold: 5
            searchStyle: GET
         prefetch:
            maxUriLength: 8000

