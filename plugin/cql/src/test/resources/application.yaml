hapi:
   fhir:
      cql:
         use_embedded_libraries: false
         engine:
            debug_logging_enabled: true
            options: "EnableExpressionCaching"
         translator:
            analyzeDataRequirements: true
            collapseDataRequirements: true
            options: "EnableResultTypes"
